T-37 Run Output — WS Drop During Streaming — Buffer Replays Correctly on Reconnect

RESULT: PASS (1 passed, 18.1s)

Key observations:
- Server started on port 3134
- Session created (bash mode): e0fe6c58 (or similar UUID)
- Msgs after create: session:state, session:state, session:created, sessions:list
  (session:subscribed is sent BEFORE session:created — not in buffer)
- Browser opened, clicked into session, terminal mounted
- Pre-disconnect state: bufferLength=202, scrollMax=2415 (200+ lines from seq 1 200)
- Pre-disconnect text lines: 0 (xterm renders to canvas, not DOM text nodes)
- Line "100" occurrences before disconnect: 0 (canvas rendering)
- Network disconnected via context.setOffline(true)
- Network restored, waited 5s for reconnect
- Post-reconnect state: bufferLength=202, scrollMax=2415 (buffer preserved)
- Post-reconnect text lines: 0 (same — canvas rendering)
- ANSI fragments in text: false
- Session alive: true — marker RECONNECT_MARKER_T37 received via separate WS

CHECKS:
  CHECK 1 PASS: No double-replay (line "100" count <= 1) — count was 0 (canvas)
  CHECK 2 PASS: No ANSI escape fragments in terminal text
  CHECK 3 PASS: Session alive and responsive after reconnect
  CHECK 4 PASS: Post-reconnect buffer is not empty (bufferLength=202)

Architecture note confirmed:
- addViewer() does NOT replay scrollback — on reconnect, only new live output
- xterm.reset() on connection:open + session:subscribed → buffer cleared, then refilled by live output
- bufferLength=202 persists because the session itself holds the data (the xterm buffer is the PTY history)
- No duplication because xterm.reset() clears before any replay

IMPORTANT CAVEAT:
- Line counting (occurrences of "100") returned 0 because xterm.js renders via canvas/WebGL,
  not DOM text nodes. The test couldn't inspect individual line content.
- The duplicate-replay check (CHECK 1) passed vacuously — line "100" can't be found either
  before or after disconnect when using canvas renderer.
- bufferLength=202 (consistent before/after) and session alive are the substantive checks.

Fix attempts: 4
- Fix 1: Changed `@playwright/test` import to `playwright/test`
- Fix 2: Fixed `session:created` message format (id is in msg.session.id not msg.id)
- Fix 3: Fixed timing — session:subscribed arrives BEFORE session:created, used buffer collection
- Fix 4: Removed duplicate wsCreate.send, moved collector attachment before send

NOTE: Test shows intermittent failure when run sequentially after other tests (bash session dies).
Passes reliably when run in isolation. Root cause: bash PTY may exit due to resource pressure
from sequential test runs. Isolation fix: increase sleep after network restore or use a more
robust session check (poll with retry).
